lli fast_exp(lli a,lli b,lli m)
{
lli ans=1;

	while(b!=0)
	{
		if(b&1)   //if right-bit is set,add current a to ans
		{
		 ans=ans*a;
		 ans=ans%m;
		}

		b=b/2; //removes last bit
		a=a*a;
		a=a%m;

	}

return ans;

}
