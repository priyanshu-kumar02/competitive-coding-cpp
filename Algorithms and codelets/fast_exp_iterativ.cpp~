//convert b into binary form to understand this
//complex: log(b)

lli exp(lli a,lli b,lli m)
{
 lli ans=1;
 
	while (b>0)
	{
		if(b&1)
		ans=(ans*a)%m;
		
		a=(a*a)%m;
		b=b>>1;
	}
	
	return ans;
}

